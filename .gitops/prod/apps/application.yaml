apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dk-data-prod
  namespace: argocd
  labels:
    tenant: dk-data
    environment: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: dk-data

  source:
    repoURL: https://github.com/data-kinetic/dk-data-fe.git
    targetRevision: main
    path: k8s/overlays/prod

  destination:
    server: https://kubernetes.default.svc
    namespace: dk-data-prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=false
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
    # Doppler operator adds defaults (host, processors, verifyTLS) and
    # strips tokenSecret.key, causing perpetual OutOfSync
    - group: secrets.doppler.com
      kind: DopplerSecret
      jqPathExpressions:
        - .spec.host
        - .spec.processors
        - .spec.verifyTLS
        - .spec.tokenSecret.key
