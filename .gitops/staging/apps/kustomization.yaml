apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # project.yaml removed — AppProject is shared between prod and staging,
  # owned by dk-data-bootstrap-prod to avoid tracking-id conflicts
  - application.yaml
