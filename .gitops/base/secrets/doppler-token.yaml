# Doppler Token Secret Placeholder
# Feature: 002-production-readiness
# Task: T014
#
# This secret holds the Doppler service token that authenticates with Doppler.
# The actual token value should be injected via:
#   - CI/CD pipeline secret
#   - kubectl create secret (manual)
#   - External secrets operator
#
# DO NOT commit actual token values to this file.
---
apiVersion: v1
kind: Secret
metadata:
  name: doppler-token-dk-data-fe
  labels:
    app.kubernetes.io/name: dk-data-fe
    app.kubernetes.io/component: secrets
    app.kubernetes.io/managed-by: manual
type: Opaque
stringData:
  # Placeholder - replace with actual token during deployment
  # Create token: doppler configs tokens create --project dk-infrastructure --config prod --name dk-data-fe-prod
  serviceToken: "PLACEHOLDER_INJECT_VIA_CI_OR_KUBECTL"
