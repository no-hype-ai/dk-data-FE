apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dk-data-rate-limit
  labels:
    app.kubernetes.io/part-of: dk-data
spec:
  rateLimit:
    average: 100
    burst: 50
    period: 1m
