# DrugBank Fetch CronJob
# 011-datasource-integration
# Fetches drug entries from DrugBank XML (credential-gated)
apiVersion: batch/v1
kind: CronJob
metadata:
  name: fetch-drugbank
  labels:
    app: fetch-drugbank
    app.kubernetes.io/name: fetch-drugbank
    app.kubernetes.io/component: ingestion
    app.kubernetes.io/part-of: dk-data
spec:
  schedule: "0 17 1 * *"  # 1st of month, 5 PM UTC
  concurrencyPolicy: Forbid
  startingDeadlineSeconds: 3600
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      ttlSecondsAfterFinished: 86400
      activeDeadlineSeconds: 7200  # 2 hour timeout (DrugBank XML is large)
      template:
        metadata:
          labels:
            app: fetch-drugbank
            app.kubernetes.io/component: fetcher
            app.kubernetes.io/part-of: dk-data
            team: data-platform
            service: dk-data
            product: dk-data
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            fsGroup: 1000
            seccompProfile:
              type: RuntimeDefault
          imagePullSecrets:
            - name: ghcr-credentials
          containers:
            - name: fetch-drugbank
              image: ghcr.io/data-kinetic/dk-data-fe/job-trigger:latest
              command: ["python", "-m", "dk_data.ingestion.main"]
              args: ["drugbank"]
              envFrom:
                - secretRef:
                    name: dk-data-secrets
              env:
                - name: OTEL_EXPORTER_OTLP_ENDPOINT
                  value: "http://alloy.infra.svc.cluster.local:4317"
                - name: OTEL_ENABLED
                  value: "true"
                - name: POSTGRES_HOST_DIRECT
                  valueFrom:
                    secretKeyRef:
                      name: dk-data-secrets
                      key: POSTGRES_HOST
                # Per-source credential (Horizon 3 §D.5, issue #319) —
                # DRUGBANK_API_KEY is pulled from the dedicated
                # dk-data-secrets-drugbank Secret, NOT the monolithic
                # dk-data-secrets. Rotating DrugBank's key touches this
                # Secret only; no other source is affected. Explicit env
                # overrides any inherited value from envFrom above.
                - name: DRUGBANK_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: dk-data-secrets-drugbank
                      key: DRUGBANK_API_KEY
                      optional: true
              resources:
                requests:
                  memory: "256Mi"
                  cpu: "100m"
                limits:
                  memory: "512Mi"
                  cpu: "300m"
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop: ["ALL"]
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir: {}
